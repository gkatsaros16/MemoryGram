$('#notifications').append("<%= escape_javascript(render 'notifications')%>");
$('#paginator-notifications').html("<%= escape_javascript(link_to_next_page(@notifications, 'LOAD MORE', remote: true, id: 'load_more'))%>");
if (!$('#load_more').length) { $('#paginator-notifications').remove(); }
